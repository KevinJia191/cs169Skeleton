Run this application on by creating a Heroku app via "heroku create" at the top level directory. You must configure a PostgreSQL database as well.

Our current running Heroku App is located at http://rocky-escarpment-7553.herokuapp.com/

To run functional tests and unit tests, see TESTREADME.txt
